/*
Hello world in C, heavily documented.
by Ricky LeDew (meltedchocolate)
*/
#include <stdio.h> //include the standard I/O so we can print things out

int main() { //Main execution, is integer because it has to return an integer status code
  printf("Hello, world!"); //Print out the message
  return 0; //Say to the system that the program exited successfully (status code 0)
}
